<div class="uom-progress-indicator">
  <div class="uom-progress-indicator__count">Step {{active}} of {{steps.length}}</div>
  <div class="uom-progress-indicator__bar">
    {{#each steps}}
    <div{{#if this}} class="active"{{/if}}></div>
    {{/each}}
  </div>
</div>
