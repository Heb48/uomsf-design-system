<input {{#if type}}type="{{type}}"{{else}}type="button"{{/if}} class="uom-button uom-button--{{style}}{{#if isFullWidth}} uom-button--full-width{{/if}}"{{#if isDisabled}} disabled{{/if}} value="{{text}}">
